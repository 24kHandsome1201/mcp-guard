{
  "name": "mcp-guard",
  "private": true,
  "version": "0.0.0",
  "description": "Preflight governance tool for MCP servers and registries.",
  "scripts": {
    "build": "corepack pnpm -r run build",
    "test": "corepack pnpm -r run test",
    "typecheck": "corepack pnpm -r run typecheck",
    "lint": "corepack pnpm -r run lint",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "@types/node": "^22.15.32",
    "eslint": "^9.25.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "vitest": "^3.0.8"
  }
}
